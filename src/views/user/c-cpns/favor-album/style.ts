import styled from "styled-components"

export const FavorWrapper = styled.div`
  .menu-list {
    display: flex;
    flex-wrap: wrap;
    margin-top: 20px;
    margin-left: -50px;
    .item {
      margin-left: 50px;
      width: 140px;
    }
  }
`
